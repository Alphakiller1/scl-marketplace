/**
 * Rank-mode row primitives live in leaderboard.tsx (client table).
 * Re-export for existing import paths.
 */
export {
  LeaderboardMobileCard,
  LeaderboardRow,
} from "@/components/scl/leaderboard";
