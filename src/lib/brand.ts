/** Public product identity. Keep legal entity wording in policy documents separate. */
export const SCL_BRAND_NAME = "Sports Cappers Leaderboard";

/** The public domain printed on share graphics (never a preview host). */
export const SCL_PUBLIC_DOMAIN = "sportscappersleaderboard.com";

export const SCL_TITLE = `SCL — ${SCL_BRAND_NAME}`;
