{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npm run db:deploy; fi && npm run build",
  "crons": [
    {
      "path": "/api/cron/grade",
      "schedule": "0 8 * * *"
    }
  ]
}
